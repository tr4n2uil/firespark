/** *	@helper DataShort * *	@param data ***/FireSpark.core.helper.dataShort = function($data, $size){	var $maxlen = $size || 15;	var $len = $maxlen - 5;	return $data.length < $maxlen ? $data : $data.substr(0, $len) + ' ...';}